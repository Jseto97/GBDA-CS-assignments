void surprise2(int number){
  textSize(40); // text size
  fill(255, 255, 255); // white
  textAlign(CENTER);
  text(number, w/2, h/2);
  textAlign(CENTER,TOP);
  text("I AM", w/2, h/2);
  text("NOT", w/2, h/2 + 50);
  text("GOOD", w/2, h/2 + 100);
  text("AT CS", w/2, h/2 + 150);
}


  